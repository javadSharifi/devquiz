/* ============================================================
 * DevQuiz — globals.d.ts
 * Build-time constants injected by esbuild --define.
 * ============================================================ */

import type { PlatformName } from './platform/types.js';

declare const __PLATFORM__: PlatformName;
