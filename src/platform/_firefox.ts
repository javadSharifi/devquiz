/* ============================================================
 * DevQuiz — platform/_firefox.ts
 * See _chrome.ts for the rationale. This file is the source
 * of truth for the Firefox build's `platform` export.
 * ============================================================ */

export { firefoxAdapter as platform } from './firefox-adapter.js';
